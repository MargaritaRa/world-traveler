
function Favorites(){
    // function Favorites({countriesId,userId, onDelete}){
    return(
        <div>
            <h1>Favorites Page</h1>

            {/* {inCart ? (
                <button onClick={() => onDelete(countriesId)} className="btn-delete">Remove from favorites</button>
            ) : (
                <CartFunction userId={userId} countriesId={countriesId} />
            )} */}
        </div>
    )
}

export default Favorites